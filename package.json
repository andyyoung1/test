{
  "name": "unifi-status-mcpb",
  "version": "0.3.3",
  "description": "MCP bundle for UniFi cloud + local Network + local Protect APIs",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "build": "tsc",
    "smoke-test": "node --import tsx src/smoke-test.ts",
    "pack": "npm run build && mcpb pack",
    "clean": "rm -rf server node_modules"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "undici": "^6.20.0"
  },
  "devDependencies": {
    "@anthropic-ai/mcpb": "^2.1.2",
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18"
  }
}
